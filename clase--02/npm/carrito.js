/** CREAREMOS UN CARRITO DE COMPRAS **/

// Creamos un carrito de compras con un array vací
const carrito = [];

// Creamos un producto
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
}

// Agregamos el producto al carrito
carrito.push(producto);

// Agregamos otro producto al carrito
carrito.push({
    nombre: "Teclado",
    precio: 50,
});

